# 后续计划（2026-08 → 2027-03 结题）

> **2026-08 重写**：上一版铺得太开（六项前置、五组对照、双相机、单目深度、
> 种子方差，合计 ~110 h），主线被淹没。本版收敛到**一条主线、五次训练**。
> 被砍掉的部分与理由记在第 6 节，需要时再捡回来。

## 0. 要保护的那一句话

```
        ┌────────────────────────────────────────┐
        │   池化用的坐标系 ＝ 位置编码用的坐标系   │
        │      Pooling Coord = PE Coord          │
        └────────────────────────────────────────┘

                   Metric 4D Space
                          │
              ┌───────────┴───────────┐
              ↓                       ↓
         Patch Pooling            4D RoPE
              │                       │
              └───────────┬───────────┘
                          ↓
                  同一套 (t, x, y, z)
```

**这比"4D RoPE"本身重要。** 理由见 1.1：patch 级时空池化有人做过（STTM），
度量 3D 位置表征有人做过（SpatialVLA / HiST-VLA），3D 坐标上做 RoPE 也有人做过
（STRING）。**没人做的是把这两件事放进同一个度量 4D 坐标系里耦合起来。**
写作时这是标题句，不是某一节的小结。

落到流程上：

```
K 帧 → K×256×d → 反投影出 4D 坐标 → 按该坐标池化到 N 个 token
                                    → 按同一坐标施加 4D RoPE → LLM
```

**核心主张**：固定 token 预算下，池化与位置编码共用**度量 4D** 坐标，
优于共用**图像网格 + 时间**坐标。

⚠️ **这个框是强命题，要有专门的实验撑。**
"度量 4D 比图像网格好"只是**弱命题**；"两者一致本身带来增益"才是框里写的那句。
G4 vs G3 只检验前者——必须加**错配臂 M2**（度量池化 + 网格 PE）才能检验后者。
设计见 3.0.2。**没有 M2，标题句就只能降级写。**

⚠️ **实现上先做最小版本，不要一上来把算子锁死**（§2.2）。
"ToMe + 新颖度打分 + λ 几何项 + size 加权质心"是不是最终形态，现在还不知道。

---

## 1. 定位：我们卡在三条线中间

| | 做法 | 缺口 |
|---|---|---|
| **VLA-4D** (2511.17199) | 历史序列 + 3D 位置嵌 1D 时间 → 4D embedding，动作侧扩 delta-time | **不做任何采样/池化**，token 数随 K 线性膨胀 |
| **4D-VLA** (2506.22242) | memory bank + **frame-wise** 下采样，相对位置编码 | **粒度太粗**：丢帧就丢时间分辨率，且帧内空间冗余完全没动 |
| **STTM** (2507.07990, ICCV'25) | 帧内四叉树合并 + 沿时间定向配对合并 | **patch 级时空池化已经有了**，但在 **2D 图像网格 + 时间**里；Video LLM，非 VLA |

**我们的位置**：保全部 K 帧（时间分辨率不损失），在帧内 + 跨帧做 patch 级池化
（空间冗余去掉），且池化与位置编码共用**度量 4D 坐标**。

### 1.1 ⚠️ novelty 必须精确表述，否则一句话被打掉

2026-08 文献核查的三条硬事实：

1. **patch 级时空池化不新** —— STTM 已做（2D+时间，免训练，Video LLM）
2. **VLA 里注入 3D 位置不新** —— SpatialVLA（深度估计器→patch 位置编码）、
   HiST-VLA（反投影到 3D，MLP 加性编码）、GST-VLA（2603.09079）
3. **⚠️ 3D 坐标上做 RoPE 不新** —— STRING 系列已用于机械臂，真机抓取 65%→74%，
   且强调平移不变性对操作任务的价值

所以**不能说**"我们把 RoPE 用到 3D/4D 坐标上"，也不能把"4D RoPE"当卖点——
那是 STRING 已经占掉的位置。能站住的只有交集，也就是 §0 那个框：

> **池化与位置编码共用同一套度量 4D 坐标系。**
> 合并后 token 的位置就是各成分的度量质心 (x̄, ȳ, z̄, t̄)，RoPE 直接吃这个质心，
> 中间没有任何坐标系转换。
> **据我们所知**（to the best of our knowledge），现有工作里这两件事各做各的——
> 池化的（STTM）不管合并后 token 的位置语义，编码位置的
> （SpatialVLA / HiST-VLA / STRING）不池化。

这个耦合不是把两个模块拼在一起，是**互相需要**：
没有质心就没有 4D RoPE（`docs/04` §4.3 的算子选型是架构约束，不是性能取舍），
而池化若不在度量空间做，质心就没有几何意义（1.2 ①③）。

**这才是论文的论点。4D RoPE 只是它的实现手段之一。**

#### 表述纪律：三句不能写、一句可以写

| ❌ 不能写 | 为什么 |
|---|---|
| "首次提出 4D token pooling" | STTM 已做 patch 级时空池化 |
| "首次提出 4D RoPE" | STRING 已在 3D 坐标上做 RoPE |
| "没人做过这个耦合" | 绝对声称，一篇没检索到的论文就打掉 |

✅ **可以写**：「据我们所知（to the best of our knowledge），**度量 4D 坐标同时
用于 token 池化与位置编码**这一设计尚未被系统研究。」

范围限定到"同时用于"这个**设计选择**，而不是任何一个组件；
再用 "to the best of our knowledge" 兜住检索的不完备性。
这两层加起来，即使日后被指出某篇相关工作，损失的也只是"尚未被系统研究"
这半句，不动摇实验本身。

⚠️ **但论点越强，越需要专门的实验。**"两者一致"是强命题，
而 G4 vs G3 比的是**两组都一致、只是坐标系不同**——它撑不起这句话。
撑得起的是**错配臂**：度量池化配网格 PE（M2）。见 3.0.2 的 2×2。

### 1.1b 轨迹质量分析的定位：motivation / diagnostic，不是第二条主线

`docs/05` §4 那一整套（jerk、SPARC、路径效率、idle 比例、跨 suite 趋势）
**只作两个用途**：

1. **motivation** —— "Long 的归一化 jerk 是 Spatial 的 18.7 倍"用来说明
   长程任务确实更需要时序信息，放 Intro 一段；
2. **diagnostic** —— 主实验若出现"成功率相同但轨迹质量不同"，用它解释机制。

**不作为独立贡献，不进主结果表，论文里不开专门一节。**
它有 500 局 × 四 suite 的数据，写起来很诱人，但那会让论文出现第二条主线，
读者分不清核心命题是什么。**一篇论文只能有一个论点。**

### 1.2 为什么"4D"不是包装：三条可证伪的理由

> ⚠️ 本节替换 `docs/04` §3 的旧论证。旧论证是「2D 剪枝隐含静态相机假设，
> 腕部相机打破它」——但 OpenVLA 在 LIBERO 上只用第三人称静态相机，
> 静态相机下图像坐标跨帧本就可比，**旧论证在本设置下不成立**。
> 下面三条**在单个静态相机下依然成立**，因此腕部相机被移出主线。

**① 深度阻止错误合并（池化侧）**

图像网格里，两个 patch 在同一 (h,w)、不同帧，会被判为"同一位置"而合并。
但机械臂**不断从物体前面划过**：t−5 帧那个格子是背景，t 帧是夹爪。
合出来的 token 不代表任何真实表面。度量坐标下 z 相差十几厘米，直接分开。
→ **这是遮挡/去遮挡频繁发生的操作任务里最实际的收益。**

**② 度量坐标与操作相关的空间关系更对齐（编码侧）**

合并后 token 的位置是质心。RoPE 通过内积产生相对性，`q·k` 只依赖位置之差——
所以"质心距离"这个量会直接进到注意力里。图像网格下它是**像素距离**，随视角变化；
度量坐标下它是**厘米数**。

> ⚠️ **这条的表述必须克制，它是三条里最弱的一条。**
>
> 能说的：度量坐标提供了一种**几何感知的关系表征，与操作任务里真正起作用的
> 空间关系更对齐**（more directly aligned with manipulation-relevant
> spatial relationships）。
>
> **不能说**："RoPE 的距离就是动作距离，所以一定更适合动作预测。"
> 这条因果链太强。审稿人一句**"视觉 token 之间的 3D 欧氏距离，凭什么等同于
> 动作空间里的距离？"**就能问倒——而实验若不够漂亮，我们答不上来。
>
> 写作时把"更对齐"和"就是"这两种说法分清楚，别在正文里滑过去。

> 这条同时解释了 ETA-VLA 为何**刻意 RoPE-free**（`docs/04` §2.5）：
> 在 2D 网格上 RoPE 的距离偏置是噪声。**同一机制换个坐标系，从缺陷变成特性。**

**③ 预算分配有了物理判据**

"离夹爪/目标近的 patch 重要"在度量空间可以直接说；图像网格里说不了——
一个远处背景 patch 在像素上可以紧挨着夹爪。

**①③ 属于池化，② 属于编码。三条都指向同一套坐标——这就是耦合的理由。**

⚠️ 注意 **G4 vs G3 直接检验的是 ①③，不是 ②**。②（对齐得更好）是解释性的，
本实验设计**证伪不了它**，所以它只能作为动机写，不能作为结论写。

---

## 2. 方法：要实现的四件事

### 2.1 K 帧输入构造 ✅ 已实现

`src/data/kframe.py`（4 项 CPU 自检）。官方 RLDS 只支持连续窗口，
这里换掉分窗函数做到带 stride。两个坑写在该文件头，都不会报错：
**动作轴必须钉死成"当前 + future"**（否则 `action[0]` 变成 K 步前的动作），
**`frame_pad_mask` 必须传下去**（§4.1）。

### 2.2 patch 级 4D 池化 ✅ Phase 1 已实现

`src/pooling/coord_pool.py`（7 项 CPU 自检）。**G2/G3/G4/M2 共用这一份，
差别只有传进去的坐标张量**——连"两组实现不一样"这种质疑都堵死。
算子内部的设计与取舍写在该文件头，这里只留三件文档层面要记住的事。

#### ① 为什么 Phase 1 不用 ToMe：λ 是 G3/G4 的混淆项

```
G4:  sim = cos(Eᵢ,Eⱼ) − λ‖pᵢ−pⱼ‖₂          p 的单位是米
G3:  sim = cos(Eᵢ,Eⱼ) − λ‖(h,w)ᵢ−(h,w)ⱼ‖₂   单位是格子数
```

**量纲不同，最优值必然不同。** 两边取同一个数值荒谬；各自调优则被反问
"G4 赢是不是因为你把它的 λ 调得更好？"——**核心对照被一个超参毁掉。**

Phase 1 的硬分箱守住的规矩是**"没有在两臂之间取值不同的自由参数"**：
空间轴箱数由预算按确定性规则求出，时间轴箱数 `n_t` 是显式的无量纲整数、
两臂同值、作为消融整组扫描。

#### ② 判读必须非对称

Phase 1 用的正是 `docs/04` §4.3 判为"只作 baseline"的均值池化。
若算子粗到两组都被**地板效应**压住，差值会被压向 0 造成假阴性。

| 结果 | 怎么判 |
|---|---|
| **G4 > G3** | **强结论**——坐标系的作用大到连粗算子都掩盖不住 |
| **G4 ≈ G3** | **不能判死刑**，必须升级到 Phase 2 的 ToMe 再测一次（§3.2 排查③） |

#### ③ 占用不对称，要主动写进论文

G3 的图像网格箱永远满占；G4 的度量体素天然稀疏（patch 全贴在物体表面）。
合成深度下 G4 只用掉 161/256 槽而 G3 用掉 242，`enforce_n` 拉平后公共预算
被 G4 拖低。**真值深度缓存好后必须重测**；仍偏低则调 `metric_extent` 的包围盒。

> 这等于让 G4 获得了**内容自适应的分辨率**（物体密集处箱多，空处不占预算）。
> 这不是混淆项，是**度量坐标的固有性质**——离开度量空间就没有稀疏性可言，
> 所以它属于处理效应的一部分。但必须明写，不能让它作为"没说的优势"藏在实现里。

#### Phase 2 · 只在 Phase 1 分出差距之后才做

按顺序单独上线，**每次只加一样**，各自留消融位：

| 顺序 | 加什么 | 为什么排这个位置 |
|---|---|---|
| 1 | ToMe 软匹配替掉硬分箱，size 加权质心 | 内容自适应，最可能带来增益 |
| 2 | 新颖度打分（去掉当前帧的冗余副本） | 独立于算子，可单独开关 |
| 3 | 当前帧 anchor 保护 | 同上 |
| 4 | 相似度里的几何项 λ | **最后加，且必须在 G3/G4 上做同样的扫描并报告曲线** |
| 5 | **M3 = 度量池化 + MLP 加性编码**（HiST-VLA 那套） | 答"为什么不用 HiST-VLA 的编码器"。⚠️ 它检验 **RoPE vs MLP 这个机制**，两边坐标系仍一致——**回答不了强命题**，别和 M2 混（3.0.2） |
| 6 | **z 二值化（前景/背景）** | 答"既然跳变以前景/背景为主，二值化是不是就够了"（`docs/05` §8.6）。现在不做：会引入阈值这个自由超参 |

`docs/04` §4.1–4.3 有这些的完整设计——**那是候选方案，不是定案。**

> ⚠️ **Phase 2 的准入条件：核心命题已经成立，且简单版本不够强。**
> 不满足就一个都不加。两种要拒绝的动机：
> · "简单版已经有效，但再加两个模块 novelty 更足" —— **不加**。
>   审稿人问"每个模块各贡献多少"时，多一个模块就多一组必须跑的消融。
> · "简单版没赢，加个模块救一救" —— **更不能加**（见 3.2b）。
> 唯一正当的动机是：Phase 1 赢了，但幅度小到需要更好的算子来把结论做扎实。

> **两件与算子无关、换算子也不能丢的事**（已在 `coord_pool` 里实现并自检）：
> 输出必须**按质心字典序排序**（乱序等于给模型喂没见过的排列，
> 测出来的掉点会是排列扰动而非信息损失，`docs/04` §4.6）；
> 顺序必须是**先池化 → 算质心 → 再施加 RoPE**（RoPE 是旋转，
> 把旋转角不同的向量做平均会收缩模长、破坏相位，`docs/04` §5.4 坑 1）。

### 2.3 4D RoPE：**扩展**而非替换 1D RoPE ✅ 已实现

`src/pooling/rope4d.py`（6 项 CPU 自检）。约 30% 的 rotary pair 改由 (t,x,y,z) 驱动，
其余 70% 保持原始 1D RoPE 不动；文本 token 在 4D 通道上取恒等旋转。
数值验过：**q·k 在坐标整体平移下不变**（§1.2② 全部压在这条上，试过 +1/−3.5/+100），
文本位置对存活 token 数不敏感。

#### ⚠️ 一条公平性要求：两臂参与旋转的通道数必须相等

G3 的坐标是 (t,h,w) 三轴，G4 是 (t,x,y,z) 四轴。若让 G3 空着 z 那批通道不转，
**G4 就比 G3 多转一批通道**——"G4 赢是因为它转的通道多"立刻成为无法排除的替代解释。

**定死**：3 轴时把 z 的份额按比例匀回 t/h/w，两臂**参与旋转的 pair 总数严格相等**。

```
head_dim=128，64 个 rotary pair
  4 轴 (G4/M2 池化侧)  [t=2, x=6, y=6, z=5]  转 19，1D 保留 45
  3 轴 (G3 / M2 PE 侧) [t=3, h=8, w=8]       转 19，1D 保留 45
```

**三件还要做的事**：

1. **拨哪些通道要做消融。** 现在从高频端取（高频通道在 2048 长的序列上早已绕圈，
   携带的可用长程信号最少），这是直觉不是证据。
2. **⚠️ 文本起始位置固定为 `K×256+1`，必须与存活 token 数无关。**
   这条直接继承阶段 1 最硬的发现：位置编码错了值 **−54 个点**（`docs/05` §7.4），
   比压缩本身（−10.6 点）大五倍。文本起点随池化率浮动的话，测的又是位置扰动。
3. **监控深层视觉 token 的注意力份额** —— 对应已测到的 36 点未解释残差
   （`keep=64` 修位置后 34.0%，等长的 `expand@64` 70.0%），
   假设是 token 同质化（`docs/05` §7.6）。

### 2.4 深度从哪来：**仿真器回放，不用单目估计**

LIBERO RLDS 只有 `image`/`wrist_image`/`state`，无深度无内外参（已核实
`configs.py:645`、`transforms.py:827`）。上一版打算跑单目深度模型（VLA-4D 用 VGGT）。

**改为：回放动作序列，直接取仿真器深度。** `measure_redundancy.py` 已经在这么做了
（评测确定性保证画面逐帧一致），代码是现成的。

- 只缓存 **patch 级深度：256 个值/帧**，不是全分辨率深度图——
  4D RoPE 要的就是每个 patch 的位置。每 suite 约 **38 MB**（全分辨率要 7.5 GB）
- **必须先验证**：抽 5 条 episode，比对回放 RGB 与 RLDS 里的 RGB。
  对不上就说明回放发散，才回退到单目深度
- 评测时同一模型在线取深度（LIBERO 是同步仿真器，只影响墙上时间不影响成功率）

> **答辩会被问"仿真真值深度是特权信息"**。三层回答：
> ① 真机通常本就有 RGB-D；② 我们的主张是**表示**，不是深度怎么来的；
> ③ 留一个小消融——在子集上换成单目估计深度，看结论是否存活。
> ③ 不进主线，但要留出接口。

---

## 3. 实验路径：六组，一张表

#### 主实验 protocol —— **开跑前锁死，不得按结果调整**

| 项 | 定值 |
|---|---|
| 数据集 | LIBERO-Long（`libero_10_no_noops`） |
| 帧数 / 间隔 | K=8 / stride=16 |
| token 预算 | **N=256**（所有臂同预算） |
| 时间箱数 | **n_t=2** |
| 训练步数 | **30 000** |
| 有效批 / 学习率 | **16** / 5e-4 恒定 |
| LoRA | r=32, alpha=16, dropout=0, 视觉主干冻结 |
| 随机种子 | **训练 seed=0；评测 seed=7**（沿用阶段 0，评测本身是确定性的，见 `docs/05` §3.2） |
| 微批 × 累积 | 有效批恒为 16。**微批本身不在 protocol 里** —— 见下 |
| 深度 | 仿真真值 |
| 评测 | **n=500 满量**，每 suite 全部 task × 全部 episode |

⚠️ **三条纪律**：

1. **主结论一律用 n=500。** 小样本只用于止损信号（§4.4 的 acc 曲线），
   **不得进入任何结论**。`docs/00` 的坑 5：n=50 已经三次误导本项目。
2. **不得在看到结果之后调整上表任何一项。** 想调的时候先问：
   如果结果是反的，我还会想调它吗？答案是"不会"就说明这是事后选择。
3. **偏离必须记录。** 真需要改（例如 30 000 步跑不完），
   在 `docs/05` 里写明改了什么、为什么、以及改动前的结果。

> 微批是**吞吐参数不是实验参数**（有效批一律 16），所以不进 protocol 表。
> 池化臂统一 `8×2`（`docs/05` §8.10 的 K=8 微批扫描），G0 用 `16×1`。

### 3.0 ⚠️ 先定死 N=256，因为它同时消掉两个混淆

**N=256 = G0 的 token 数。** 这个选择不是随便挑的，它让**每一组都在同一预算上**，
包括单帧基线——于是"历史帧有没有用"和"坐标系哪个好"可以在同一张表里回答。

连带的好处：**G2 与"等预算历史对照"合并成同一组**，少一次训练。

| 组 | 输入 | 池化 | 池化坐标 | PE 坐标 | token | 角色 |
|---|---|---|---|---|---|---|
| **G0** | K=1 | — | — | 1D | 256 | 单帧基线 ✅ Spatial **74.0%**(n=500) |
| **G1** | K=8 | **无** | — | 1D | 2048 | **VLA-4D 类**；上限参考 |
| **G2** | K=8 | **帧独立**，每帧→32 | (h,w) | (t,h,w) | 256 | 粒度对照 **＋等预算历史对照** |
| **G3** | K=8 | **patch 级跨帧** | (t,h,w) | (t,h,w) | 256 | **STTM 类**，一致 |
| **G4** | K=8 | **patch 级跨帧** | **(t,x,y,z)** | **(t,x,y,z)** | 256 | **本方案**，一致 |
| **M2** | K=8 | **patch 级跨帧** | **(t,x,y,z)** | (t,h,w) | 256 | **⭐ 主实验**（不是消融）：强命题的唯一直接证据，见 3.0.2 |
| *M1* | K=8 | patch 级跨帧 | (t,h,w) | (t,x,y,z) | 256 | *错配臂，补全 2×2，可砍* |

#### 3.0.1 ⚠️ G1 vs G0 有 token 数混淆，不能用它验证前提

G0 是 256 token，G1 是 2048——**差了 8 倍**。G1 > G0 说不清是历史帧有用，
还是单纯 token 多了 8 倍。**前提检验要用 G2 vs G0**：都是 256 token，
唯一差别是有没有历史。

G1 降级为**上限参考**：它回答"不压缩能到多少"，即 G2–G4 的压缩代价有多大。

> 残留的、无法消除的不对称要写明：G0 的 256 个 token 是**原生未池化**的
> （正是预训练时模型见过的形态），G2–G4 的 256 个是池化产物。
> 这个偏差**方向对我们不利**（偏向 G0），所以是安全的——
> 但不能假装它不存在。

#### 3.0.2 ⚠️ 标题句是强命题，G4 vs G3 只检验了弱命题

§0 的框说的是**"两者必须一致"**（强命题）。
但 G3 和 G4 **两组都是一致的**，只是坐标系不同——所以

```
G4 > G3  只能证明「度量 4D 比图像网格好」   ← 弱命题
         证明不了「一致比不一致好」          ← 强命题，也就是标题句
```

审稿人一问**"那度量池化配别的位置编码呢？"**，我们没有数据。

**修法：把错配臂做出来，构成 2×2。**

| | **PE 用 (t,h,w)** | **PE 用 (t,x,y,z)** |
|---|---|---|
| **池化用 (t,h,w)** | **G3**（一致） | *M1*（错配） |
| **池化用 (t,x,y,z)** | **M2**（错配） | **G4**（一致） |

- **主效应**（列/行）= 弱命题：哪个坐标系好
- **交互项** = **强命题：一致本身是否带来增益**
- 只做 M2 就够撑强命题（G4 > M2 且 G4 > G3）；补上 M1 才是完整的因子设计，
  能报交互项的显著性。**M1 是预算不够时第一个砍的**

> ⚠️ **M2 不是消融，是主实验。** 它和 G4 的关系不是"再验一个变量"，
> 而是**标题句成立与否**：G4 > G3 只说明度量坐标好用，G4 > M2 才说明
> "两者一致"这件事本身有价值。论文的 **Table 1 必须包含 M2**，
> 与 G0–G4 并列，而不是塞进附录的消融表。
> 排期上 M2 与 G4 **同等优先**，谁都不能因为"先看看 G4 赢不赢"而后置。

> 实现代价很低：**池化代码一行不动**，只换 PE 那一侧传进去的坐标张量。
> §2.2 的 `pool(E, coords, budget)` 接口本来就把两者解耦了。

⚠️ 另有一组**别混为一谈**：`M3 = 度量池化 + MLP 加性编码`（HiST-VLA 那套）。
它检验的是 **RoPE（旋转、相对）vs MLP（加性、绝对）这个机制**，
两边坐标系**仍然是一致的**——所以它回答不了强命题，是另一个问题。
放进 Phase 2 消融，用来答"为什么不用 HiST-VLA 的编码器"。

#### 3.0.3 G2 的规格必须写死，否则 G3 vs G2 是两个变量

早先只写"抽 K′ 帧 + frame-wise 池化"，那样 G3 vs G2 同时含
**帧数不同**与**池化粒度不同**两个变量。

**定死**：G2 = **K=8 全保留**，每帧**独立**池化到 N/8 = 32，总计 256。
于是 G3 vs G2 纯隔离 **patch 级跨帧联合 vs 帧级独立**。

> ⚠️ **要如实说明：这样的 G2 不是 4D-VLA 的忠实复现。**
> 4D-VLA 丢的是整帧（时间分辨率真的损失了），而我们的 G2 保全 8 帧、
> 只是各帧独立压缩。**G2 因此是比 4D-VLA 更强的基线**——
> 赢它更有说服力，但不能在论文里把它标成"4D-VLA"。

#### 3.0.5 实现规格：三件必须写死的事

不写死的话，每一件都会悄悄变成第二个 λ。

**① M2 的网格坐标 = 箱内 patch 的 (t,h,w) 算术均值**

M2 是度量池化、网格 PE。一个度量体素跨多帧时，箱内 patch 的 (h,w) 可能差很远
（同一物体运动导致投影偏移）。

**定死**：三个分量 (t̄, h̄, w̄) 全部取箱内**算术均值**，与 G4 取度量质心
**用同一套平均逻辑、同一份代码**。不要用中位数、投影中心或"取最近帧的位置"——
那会引入新的设计变量，而这个对照的全部价值就在于**池化侧完全相同**。

> ⚠️ 运动物体的箱里，平均出的 (h̄,w̄) 可能落在既不是起点也不是终点的位置。
> **这是预期行为，不是 bug**——它正是"错配"要暴露的退化。
> 代码里加注释，免得后来者"顺手修好"。

**② G2 用与 G3/G4 相同的分箱算法，只是不在 t 轴上分**

G2 是帧独立池化。**不要给它单独写随机采样或均匀裁剪**——那样 G3 vs G2
会同时差"跨帧联合"和"池化算法"两个变量。

**定死**：G2 = 每帧各自在 h–w 平面上分 32 箱、箱内平均，共 8×32 = 256。
与 G3 的唯一差别是 **t 轴不参与分箱**。

**③ ⚠️ G3 的分箱必须真的跨 t 合并，否则 G3 ≡ G2**

这是顺着 ② 推下去撞到的坑，**比前两条严重**：

G3 要把 2048 个 patch 分成 256 箱。若轴划分取 `t:8 × h:8 × w:4 = 256`，
**t 轴每帧一箱 = 完全不跨帧合并**——那 G3 就是 G2，粒度对照测了个寂寞，
而且不会报错，只会得到"G3 ≈ G2"然后被误读成"跨帧联合没价值"。

**⚠️ 2026-08 更正：这一条曾写成"归一化坐标下各向同性分箱，唯一的标量是箱边长"。
代码写出来跑了一遍，这个规则不成立。**（`src/pooling/coord_pool.py`，CPU 自测）

实测：K=8、N=256 下让贪心自由细化，各轴箱数落在

```
G3 (t,h,w)    → [7, 6, 6]      跨帧 token 仅 36/252 = 14%，最大跨度 2 帧
G4 (t,x,y,z)  → [6, 6, 5, 5]   跨帧 token 仅 23/251 =  9%，最大跨度 2 帧
```

**t 轴只有 8 个取值，各向同性会把它推到接近满分辨率，于是两组基本都在做逐帧的
空间池化。** 三个后果连着倒：

1. "静止表面在多帧可见 → 塌成一个带 size 的 token"这个机制**根本没发生**
2. G3 几乎不跨帧 → **G3 ≈ G2**，粒度对照空转
3. §1.2① 的"同一 (h,w) 不同帧可能是不同表面"这个**错误合并机制触发不了**
   → G4 vs G3 也就没什么可赢的，**主实验近乎没有功效**

而这**不报任何错**。原先设计的断言（"存在跨帧 token"）在 14% 下照样通过。

**改用的规则**：

- **t 轴箱数 `n_t` 显式给定**，不交给贪心。默认 `n_t=2`（K=8 时每箱覆盖 4 帧）
- 空间轴仍按贪心细化：每次把当前最粗的轴 +1，**且非空箱数必须真的增加**
  （只判 `≤ budget` 会死循环——箱数超过数据分辨率后占用不再增长，
  贪心永远认为还能细。这个也是跑出来才发现的）
- **`n_t` 不是 λ 那类混淆项**：λ 的问题是两个坐标系量纲不同（米 vs 格）、
  没法取同一个值；`n_t` 是**无量纲整数，G3/G4/M2 取同一个值**，
  并作为消融整组扫描 `{1,2,4,8}` 同时报告两臂曲线。
  要守的规矩是"**没有在两臂之间取值不同的自由参数**"，这条仍然成立

**断言也跟着改**：阈值必须是**比例**（默认跨帧 token ≥ 25%），不能只查 `> 0`。
`assert_cross_frame_merge()` 已实现，自测里有一条专门验证它能拦下 14% 那种情况。

> 这一条是"先写代码再改文档"换来的。纸面上"各向同性"读着很有原则，
> 跑一遍才知道它会让主实验静默失去功效。

### 3.0.4 每一对比较隔离出什么

| 对照 | 隔离出什么 | 若打平意味着 |
|---|---|---|
| **G2 vs G0** | **同预算下历史帧有没有用**（前提检验） | 前提不成立，整个方案要重新解释 |
| G1 vs G2 | 8× token 预算买到了多少 | 压缩几乎无代价（好消息） |
| G3 vs G2 | **池化粒度**：跨帧联合 vs 帧独立 | 跨帧联合没有价值，第一条动机作废 |
| **G4 vs G3** | **坐标系**：度量 4D vs 图像网格 | ⚠️ **弱命题都不成立** |
| **G4 vs M2** | **⭐ 一致 vs 错配** | ⚠️ **标题句（强命题）不成立**，只能降级写弱命题 |
| *G3 vs M1* | *另一侧的一致 vs 错配* | *交互项不显著* |

**必须赢的是 G4 vs G3（弱）与 G4 vs M2（强）这两对。**
弱的赢了强的没赢 → 论文还能写，但故事从"发现一条统一性原理"降级成
"又一个坐标系选择的 ablation"，分量差很多（§6.1）。

#### ⚠️ 全部先用 Phase 1 的最小版本跑

按 §2.2：**零自由超参的坐标分箱池化**，不带 ToMe、不带新颖度打分、
**尤其不带 λ 几何项**——λ 在两个坐标系里量纲不同，各自调优等于给 G4 开小灶。

只有 Phase 1 分出差距之后，Phase 2 的每一项才逐个加上去，
那时它们是**消融表的行**，不再是核心对照的混淆项。

### 3.0.6 ⭐ Phase 1.5：三项必补，但**排在六组之后**

外部评审同时提了"补这三个 sweep"和"不要扩大实验矩阵"。**两者不矛盾，
只要把它们放在时间轴上**：六组主线先跑完，再按结果决定这三项做不做、做多大。

在此之前**一项都不开**——现在扩矩阵，等于在还不知道核心命题成不成立的时候
先花掉四十小时。

| # | 补什么 | 为什么非补不可 | 最小设计 | 成本 |
|---|---|---|---|---|
| ① | **token 预算** | 只有 N=256 的话，"提升"可能只是这一个预算下的偶然 | G3/G4 各加 **N=128** 一档（256 已有） | 2 × 30.8 h + 2 评测 ≈ 74 h |
| ② | **时间分辨率 n_t** | 核心问题是「历史帧越多，G4 的优势是否越明显」——这是机制证据，不只是超参 | G3/G4 各加 **n_t=1 与 4**（2 已有），**训 10 000 步**而非 30 000 | 4 × 6.6 h + 4 评测 ≈ 40 h |
| ③ | **深度鲁棒性** | 仿真真值深度是 oracle，审稿人必问 | **只跑 G4 一条**，深度换成单目估计，与 GT 版比 | 30.8 h + 1 评测 ≈ 37 h |

合计约 **115 h ≈ ¥230**，六组主线（~150 h）之外。全部做完约 265 h，
按每天 8 小时机时算是一个月，2027-03 结题前放得下。

**三条的判读各自不同：**

- ① **只需要"方向一致"**，不需要两个预算下增益相同。N=128 上 G4 仍 > G3、
  仍 > M2，就够堵住"只在 256 上成立"。若方向反转，那是真发现，要写进去。
- ② **要的是斜率**：G4−G3 的差值随 n_t 增大而增大，才支持"跨帧合并是收益来源"。
  若差值与 n_t 无关，说明收益来自坐标系本身而非时间维度——**这仍然支持强命题，
  但要改写机制解释**，不能含糊过去。
  ⚠️ n_t 只训 10 000 步是**刻意的取舍**：它检验的是趋势不是绝对值，
  而 `docs/05` §8.2 显示 10k 步时曲线仍在爬坡。**报告时必须写明这一点**，
  不能把 10k 的数与主表 30k 的数并排放。
- ③ **只需要"相对优势存活"**：单目深度下 G4 vs G3 的差值缩小是预期内的
  （深度噪声会损害度量坐标），**只要方向不变**就能写"本方法不依赖真值深度，
  只是在真值下更好"。若方向反转，则必须把适用范围收窄到"有 RGB-D 的场景"。

> ⚠️ **这三项都不改动主实验的任何设置。** 主表永远是 N=256 / n_t=2 / GT 深度 /
> 30 000 步 / n=500。补充实验只在附录，且各自独立成表，不与主表混排。

### 3.1 预先登记的预测

1. **G2 > G0**（Long 上，同为 256 token）。依据：Long 是多步、有阶段歧义的任务；
   且本项目自己测到 **Long 的归一化 jerk 是 Spatial 的 18.7 倍**（`docs/05` §4.4），
   轨迹极不平滑正是"不知道自己处在哪一步、反复试探"的症状。
   ⚠️ 用 G2 不用 G1——G1 多了 8 倍 token，赢了也说明不了问题（3.0.1）
2. G3 > G2（同预算下跨帧联合优于帧独立）
3. **G4 > G3**（弱命题），且**收益集中在遮挡/阶段歧义时刻**而非均匀分布 ——
   1.2 ① 预测的正是遮挡场景。**均匀分布的收益是警号**，那更像正则效应。
   ⚠️ 这检验的是 1.2 的 ①③；**②（"更对齐"）本设计证伪不了**，只能作动机
4. **G4 > M2**（强命题）。这是标题句的唯一直接证据
5. G1 > G4，但差距小于 G4 − G2 —— 即 8× 预算买到的，少于坐标系换对买到的。
   **这条若不成立（G1 远好于 G4），说明预算 N=256 卡得太死**，
   要在下结论前补一个 N=512 的点
6. ~~Spatial 上 G2 ≈ G0（单步、基本全可观测，历史帧边际价值低）~~
   **⚠️ 作废**（§4.2）：stride=16 下 Spatial 没有任何一个时刻拥有完整历史，
   历史全是补帧。这条预测会成立，但成立的原因是序列退化，**同义反复，
   不构成检验**。要保留就得给 Spatial 单独定 stride（≈5）

**分层分析比总成功率更能证伪。** 我们有 per-task 分解和逐步轨迹，做得了。

### 3.2 结果怎么判

| 结果 | 含义 | 下一步 |
|---|---|---|
| G4 > G3 **且** G4 > M2 | 强命题成立 | 主线成立，进消融与写作。补 M1 报交互项 |
| G4 > G3 但 G4 ≈ M2 | **只有弱命题** | 标题句降级成"度量 4D 坐标优于图像网格"。论文还能写，但从"统一性原理"变成"坐标系 ablation"，分量差很多。**先查 M2 是否实现有误**（PE 侧坐标真的换了吗），再认这个降级 |
| G4 ≈ G3 | 弱命题都没有 | **先排查实现，再认结果**：① 坐标归一化尺度失配（2.3 细节 2，最可能）；② 深度质量（回放是否发散）；③ N=256 太紧 → 用 3.0.6 ① 的 N=128/512 复核。**三条都排完仍打平，就按 3.2b 转向，不要靠堆模块救** |
| **G2 ≈ G0** | **前提就没成立** | 先给多帧加 4D RoPE 再测一次（阶段 1 的发现指向位置编码）；再查 stride（`docs/05` §8.7）。仍打平则换基准 |
| G1 ≫ G4 | 预算卡太死 | 补 N=512 再判，别在 N=256 上下结论 |

### 3.2b ⭐ 预先登记的转向方案（**在看到结果之前写下**）

这一节的全部意义在于**它写在数据之前**。事后再选叙事，就是 p-hacking 的
另一种形式——而且审稿人看得出来。

| 若 | 放弃什么 | 改成什么 | 手上已有的支撑 |
|---|---|---|---|
| **G4 ≈ M2** | 放弃"坐标系一致性"作为核心 claim | **geometry-aware token pooling**：度量坐标用于池化是有效的，位置编码用哪套不关键 | G4 vs G3（若成立）；`docs/05` §8.8 的机制余量 28.5% |
| **G4 ≈ G3** | 放弃整条 4D 主线 | **VLA 的时序 token 压缩与位置敏感性研究**：压缩视觉 token 时，位置编码的处理方式比信息损失重要五倍（−54 vs −10.6 点） | `docs/05` §7 已经是完整结果，n=200/500，机制诊断齐全 |

**第二个转向不是安慰奖。** §7 那个 −54 点的发现本身就是一篇完整的短文：
它有明确的机制（视觉块夹在 BOS 与语言 token 之间，压缩使整个语言块前移）、
可复现的修正（还原成分 patch 的质心，+54 点）、以及跨 suite 的验证。
四组对照（G0/G1/G2/G3）在那个叙事下**照样全部用得上**——
G2 vs G0 变成"历史帧的价值"，G3 vs G2 变成"压缩粒度"，
只是不再声称度量坐标。

⚠️ **两个转向都不允许"再加一个模块试试"。** ToMe、novelty score、anchor、λ
这些在 Phase 2 里排着队，但它们的作用是**在核心命题已经成立之后把效果做强**，
不是**在核心命题不成立时把它救活**。后者是拿自由度换 p 值，
本项目在 `docs/00` 的坑列表里已经记过三次同类教训。

唯一的例外：若 ①②③ 排查显示 **分箱粗到两组都被地板效应压住**
（判据：G3 与 G4 的槽位利用率都远低于预算，或箱内 patch 数中位数极大），
则允许**一次性**换 ToMe 复测——那是排查"算子上限"，不是加模块。
做了就要在论文里写明做过，无论结果如何。

### 3.3 ⚠️ 评测必须 n=500，不能 n=50

功效分析（`docs/05` §8.3）：n=50 只能检出 **25 个百分点以上**的差异，
而这里期待的效应量是 10–15 个点。用 n=50 等于预先注定"不显著"，
再把它误读成"方法没用"。

| 要检出 | 每组所需 n |
|---|---|
| 15 个点 | ~162 |
| **10 个点** | **~356** |

这个教训本项目**犯过两次**（"2.7× 压缩免费"被推翻；step15000 的 56% 一度被当成退化）。
**先算功效，再解读方向。**

---

## 4. 前置四项：已定死的参数

四项全部完成。**证据、统计量与踩过的坑一律在 `docs/05`，这里只留结论与未完项。**

| 参数 | 定值 | 一句话依据 | 出处 |
|---|---|---|---|
| 帧间隔 stride | **16** | 特征距离比值落在 0.5–0.8 带内；带内取最小以少补帧 | `docs/05` §8.7 |
| 帧数 K | **8** | 由 stride × 覆盖时长定。**不是显存上限**（K=16 也只要 19.2 GB） | §8.5 |
| token 预算 N | **256** | = G0 的 token 数，同时消掉两个混淆 | §3.0 |
| 训练步数 | **30 000** | 与官方的缺口在此停止显著（−4.2 点，p=0.083） | §8.2 |
| 微批 × 累积 | G0 `16×1`；池化臂 `8×2`；G1 待测 | 吞吐实测；**有效批一律 16** | §8.10 |
| 单臂用时 | 池化臂 **30.8 h**（30k 步，3.7 s/步，19.0 GB） | 真 K=8 实测 | §8.10 |
| z 包围盒 | 待 `dump_camera.py` 给 | 全跨度与剪背景差 0.7 个点，不敏感 | §8.6 |

**深度闸门 ✅ 通过**：跨帧跳变率 23.5%、跳变幅度超 z 箱宽 19.2%，两个判据都 >15%
（`docs/05` §8.6）。G4 vs G3 有物理基础，主线可以开跑。

### 4.1 三条写代码时不能忘的纪律

都来自实测，且**违反了都不会报错**：

1. **有效批跨臂固定为 16。** 微批不同、有效批相同，数值上等价；
   但若有效批跟着显存漂移，G0 vs G1 的比较作废。
   `finetune_kframe.py` 只开放 `--micro`，累积由它自动配平，除不尽直接拒绝。

1b. ⚠️ **所有臂必须用同一个 episode 子集。** 回放取深度不是每条 episode
   都能对上（实测 task 9 的两条都找不到匹配的 demo，HF 上还缺 task 1 的
   demo 文件）。若 G4/M2 只用"能对上深度"的子集而 G2/G3 用全部，
   **两者的数据量就不同，比较作废**。
   做法：先统计覆盖率，把能对上的 episode 列成一份固定清单，
   **六组全部只用这份清单**。代价是数据量减少，公平性保住。
   ⚠️ 这份清单要连同结果一起发布——它是复现的一部分。
2. **`frame_pad_mask` 一路传到池化算子。** 补帧是把第 0 帧重复若干次，
   不屏蔽的话 K 份相同内容落进 K 个 t 箱，白占预算，
   还会把 §3.0.5 ③ 那道「跨帧合并率 ≥25%」的防退化闸门撑高到失效。
3. **`metric_coords` 的相机参数必填。** 缺了它只能得到 `(t,h,w,z)`，
   那样的"G4"是带深度通道的 G3——分箱照跑、训练照收敛、对照表照填。

### 4.2 Long 是唯一站得住的主战场

stride=16、K=8 的窗口跨 113 帧，各 suite 历史完整的时刻占比：
**Long 71.1% ✅ / Object 20.0% / Spatial 0.0% ❌**（`docs/05` §8.7）。

→ §3.1 预测 4「Spatial 上 G2 ≈ G0」**作废**：它会成立，但成立的原因是历史序列
退化成了补帧生成器，同义反复，不构成对方案的检验。

→ 论文要**主动开一节「数据集适用性边界」**，用这张表回答"为什么不在所有 suite 上刷分"。
这是 LIBERO 的固有属性，不是方法的缺陷，但不能等着被问。

### ⚠️ 排期缺口：G0 必须在 libero_10 上重训一次

现有的 G0 基线（`docs/05` §8.2：74.0% → 30k 步 80.0%）是在 **libero_spatial**
上训的，而主线的主战场是 **libero_10**（§4.2）。**`G2 vs G0` 是前提检验，
跨 suite 比是无效的。**

改主线回 spatial 是死路：那上面 **0%** 的时刻拥有完整历史，历史序列全是补帧
（`docs/05` §8.7），跑出"历史没用"是同义反复。

所以只能在 libero_10 上补一次 G0。K=1 用 `16×1`，约 20 h（开跑前 `--bench_only` 量准）。

- [ ] `--arm G0 --K 1 --dataset_name libero_10_no_noops`，30 000 步

> 这笔账原先没算进排期，是写训练脚本时才发现的 —— 记在这里，
> 别在跑完 G2 之后才想起来没有可比的基线。

### 4.3 还没做完的

- [x] **相机反投影已外部验证**（`docs/05` §8.8）：两相机点云共视部分
      p10 = 0.45 cm，翻转约定 `flipped=True`。⚠️ agentview **不是跨任务常量**
      （Δpos 0.65 m），已改为按 task_id 存
- [x] 十个任务的相机全部导出；包围盒 p1–p99 =
      x[−1.21,+0.23] y[−0.90,+0.86] z[−1.10,+1.36]（`docs/05` §8.8）
- [x] **世界系直测通过**（`docs/05` §8.8）：G4 比 G3 多分开 **28.5%** 的格子，
      槽位利用率 G4 240 / G3 242 齐平。包围盒沿用 p1–p99 的**确定性规则**，
      不收紧——参数表上零个可被质疑的数
- [ ] **训练流要拿到 task 身份**才能取对相机。RLDS 的
      `task.language_instruction` 在 LIBERO 里逐任务唯一，可据此建
      instruction → task_id 的映射；深度缓存那边文件名里本来就有
- [ ] `python scripts/check_replay.py --suite libero_10 --n_episodes 5`
      —— 比对回放 RGB 与 **RLDS 训练数据** RGB（对不上后面全白搭）。
      纯仿真器不抢 GPU，可与 G2 的训练并行
- [ ] 离线缓存训练集的 patch 级深度，四 suite，约 38 MB/suite
- [x] **模型侧接线**：`src/pooling/wire.py`（7 项 CPU 自检）。六组对照全由这一份
      产生，差别只在 `arm`。⚠️ 两个必须在真模型上做的检查：
      **第一次 forward 后调 `assert_rope_active`**（挂点绑定 transformers 4.40.1，
      4.43+ 旋转嵌入上移到 LlamaModel，挂点会**静默失效**——loss 照降、结论全错）；
      以及确认序列长度与 loss 符合预期
- [x] **挂点已验**（`docs/05` §8.9）：四项全过。序列长 G1 2068 / 池化臂 276，
      rotary_emb 32/32 层；**改深度 → G3 精确不变、G4 变**（度量坐标真的进了模型）；
      **G4 与 M2 的 logits 不同**（PE 侧坐标真的起作用 = 强命题的通路存在）
- [x] **训练脚本**：`scripts/finetune_kframe.py`，六组共用、差别只在 `--arm`。
      有效批固定 16（K=1 用 16×1，池化臂 8×2）。三处与 `finetune_single.py`
      刻意保持一致：LoRA 配置、优化器、checkpoint 分步保存——不一致就没法比
- [x] **吞吐已测（真 K=8，`docs/05` §8.10）**：微批 2 → 4.4 s/步 16.6 GB；
      4 → 3.9 s/步 17.4 GB；**8 → 3.7 s/步 19.0 GB，30k 步 30.8 h**。
      与作废那轮（数据侧 K 退化成 1）对照读出来的是：
      **池化省的是显存和 LLM 那一段的时间，成本大头（视觉主干）一点不省** ——
      K=8 池化后显存与 K=1 持平（16.6 vs 16.33，序列长都是 ~294、视觉主干冻结
      分 K 次串行不留激活），时间却翻 1.87 倍（8 倍视觉前向）。压缩必须靠效果说话。
- [ ] 开 G2 长跑（30.8 h ≈ ¥62），同时并行做覆盖率统计与固定 episode 子集
- [ ] G4/M2 解锁的前置：回放保真度验证 → 训练集深度缓存 →
      RLDS 流里靠 `task.language_instruction`（LIBERO 逐任务唯一）取 task_id

> ⚠️ 已完成的深度诊断用的是**评测轨迹**（我们自己录的 action_env），测场景统计足够；
> 上面第二项要验的是**训练集**画面能否被回放复现，是另一件事，别合并。

### 4.4 早期止损信号：用 acc 曲线，不用前 100 局成功率

外部评审建议"G3/G4 各跑 100 局看趋势，不对就调头"。**动机对（11 月太晚），
方法会重演本项目已犯三次的错**：Long 基线约 50% 时，n=100 对 10 个点的效应
功效只有 **29.4%**（n=500 是 89.0%）——真有 10 点差距时七成概率看不出趋势，
据此调头是七成概率的假阴性，而这次赌的是整条主线。

**替代**：早期信号用训练侧的 **action token accuracy**。它是连续量，
样本量 = batch × 步数，比成功率大三个数量级，且训练时白送。

- [ ] G3/G4 各训 5 000 步比 acc 曲线（约 6 h ≈ ¥13），9 月中即有信号

⚠️ **只能当止损信号，不能当成功信号。** `docs/05` §8.2 已见 acc 单调上升而成功率
非单调（acc 0.500→0.643 时成功率 58%→56%）。acc 明显更差要查；
acc 更好**不代表**成功率更好。

### 4.5 论文表述：两条已经收窄的

- **Intro 第一句不写"VLA 性能崩塌的隐藏原因"**——那个现象只在**主动压缩视觉
  token 时**出现，OpenVLA 原样跑没有。应为：「在压缩视觉 token 时，位置编码的
  处理方式比信息损失重要五倍（−54 vs −10.6 点）。因此我们不试图『压缩』，
  而是『重排』——把视觉 token 映到统一的度量 4D 坐标系。」
- **z 轴主要分开前景与背景，不是物体级细结构**（`docs/05` §8.6），
  不能写成"能区分夹爪与其下方物体"。
- 配套：**正文 Table 1 只放 N=256 预算下的 G0–G4 + M2，G1（2048 token）放附录。**
  让审稿人聚焦"同预算下的坐标系之争"，而非"省了多少算力"——
  后者会被 Drop-Then-Recovery 一句话打掉（§6.1）。

---

## 5. 成本与时间

| 项 | 用时 | 费用 |
|---|---|---|
| 训练 G1–G4 + M2，**5 × 30 000 步** | **87 h** | ¥183 |
| 评测 n=500 × 6 组（含 G0 满量） | 17 h | ¥36 |
| 前置（stride / 深度 / 显存 / step17500 满量） | 7 h | ¥14 |
| **合计（主线）** | **~110 h** | **~¥232** |
| *M1（补全 2×2，可砍）* | *+15 h* | *+¥31* |
| *N=512 复核（仅在 3.2 触发时）* | *+60 h* | *+¥125* |

比上一版（66 h）多 15 h，买的是**标题句的强命题**——
没有 M2，§0 那个框就只能降级成"坐标系 ablation"。这 15 h 花得值。
仍远低于被砍掉的旧方案（110 h）。

**机时不是瓶颈，实现是。** 2026-08 → 2027-03 共 6.5 个月：

| 月份 | 做什么 | 产出 |
|---|---|---|
| 9 月 | 前置四件 + K 帧 collator + 深度反投影 + **G2 训练** | **G2 vs G0：同预算下历史帧有没有用（前提）** |
| 10 月 | **Phase 1 分箱池化** + 4D RoPE + G3、G1 | G3 vs G2：粒度 |
| 11 月 | **G4 + M2**（都是 Phase 1 最小版本） | **G4 vs G3（弱）+ G4 vs M2（强）** |
| 12 月–1 月 | **Phase 2 逐项上线**（ToMe → 新颖度 → anchor → λ）+ 其余消融（K、预算、通道分配、t̄ vs 区间）+ 分层分析 | 消融表 |
| 2 月 | 补漏 + 写作 | 初稿 |
| 3 月 | 结题 | 报告 + 演示 |

**11 月底必须同时拿到 G4 vs G3（弱）和 G4 vs M2（强）。**
那之后才有时间应对"打平"或"只有弱命题"。

⚠️ 9 月的 G2 是**前提检验**，排在最前面——它若打平，后面三组都白跑。
注意它同时也是粒度对照的基线，一组两用（3.0）。

⚠️ 排期把 **4D RoPE 提到 10 月**、和 Phase 1 池化一起上——因为 §0 的论点是
**两者共用坐标**，拆开做就没有 G4 可言。而 Phase 2 的算子优化全部压到 12 月之后，
它们是消融表的行，不在关键路径上。

---

## 6. 砍掉了什么，以及什么条件下捡回来

| 砍掉的 | 原因 | 捡回条件 |
|---|---|---|
| **腕部相机** | 引入它只为救「2D 剪枝假设静态相机」这条旧论证，而 1.2 的三条理由在单相机下就成立。代价却极大：token 预算翻倍、K 上限重测、G0 基线重跑 | 主线跑完仍有时间 → 作为"多视角 token 落在同一坐标系可直接去重"的扩展消融。这是纯 2D 方法结构上做不到的事，是加分项不是必需项 |
| **单目深度模型（VGGT 等）** | 仿真器回放能拿真值，代码现成 | §4.3 的回放保真度验证发现发散 |
| **训练种子方差（2 组 × 2 seed，24 h）** | 效应大就不影响结论，效应小才需要它 | **G4−G3 落在 ±5 点带内** → 必须补，否则无法判断是不是种子噪声 |
| **G5 假历史对照**（历史帧取自随机时刻） | 它保护的是"历史帧有用"这个前提，不是核心创新 | ⚠️ **条件已放宽但未取消**：续训至 30 000 步后基线 80.0%，与官方 84.2% 的差距已不显著（`docs/05` §8.2），头部空间比原先小，但仍有 4.2 点。**G2 > G0 一旦出现大幅提升就必须补**，用于排除"收益来自更多有效训练/正则"这一混淆。构造以**同 episode 内打乱顺序**为主（只破坏时序结构，保留场景统计），取自其他 episode 为辅 |
| **proportional attention**（ToMe 的 log(size) 偏置） | flash-attn 下无挂载点，要切 sdpa 并逐层包 `LlamaSdpaAttention.forward`，整条对比链含基线都要重跑 | 消融阶段有余力再说 |
| **四 suite 全跑** | 主战场是 Long，Spatial 只作"历史帧在单步任务上价值低"的对照 | 写作时按需补 |

### 6.1 ⚠️ 范围声明：不主张在 LIBERO 上刷 SOTA

**OpenVLA-OFT 已达 95.0%**，其收益来自**动作分块与并行解码，输入侧没有历史帧**。
评审会直接问"为什么不用 action chunking"。

所以主张**不是**"我们把 LIBERO-Long 做得更好"，而是
**固定 token 预算下，度量 4D 的池化 + 位置编码比图像网格的保留更多任务相关信息**——
表示效率的主张，与 OFT 正交，可叠加。

同一条线上还有一个警示：Drop-Then-Recovery 发现 LIBERO 上**移除一半 LLM block，
OFT 反而从 95.0% 升到 98.3%**。既然模型冗余到砍一半层都不掉点，
**"压缩视觉 token 而不掉点"本身就不是有说服力的结果**——
所以核心对照必须是 G4 vs G3（**同预算下谁更好**），而不是"压缩 vs 不压缩"。

**效率是副产品，表示才是论点。**

#### ⚠️ 若只拿到弱命题，论文怎么写

G4 > G3 但 G4 ≈ M2 的情况（3.2 第二行）要提前想好，别到时候硬撑：

| | 强命题（拿到 M2） | 弱命题（只有 G4>G3） |
|---|---|---|
| 标题句 | 池化坐标系 ＝ 位置编码坐标系 | 度量 4D 坐标优于图像网格坐标 |
| 定位 | 一条统一性原理 | 又一个坐标系选择的 ablation |
| 分量 | 可以撑主贡献 | 只够撑一节 |

**弱命题也不是没东西写**——G3 vs G2（跨帧联合 vs 帧独立）与
G2 vs G0（同预算历史帧）都是独立成立的结论。但故事要重写，
不能把弱命题包装成强命题，那是最容易被当场拆穿的一种写法。

---

## 7. 风险

1. **⚠️ G4 ≈ G3** —— 弱命题都不成立。最致命，所以排在 11 月而不是最后。
   次线（G3 vs G2 的粒度结论）能救回一部分
1b. **⚠️ G4 ≈ M2** —— 弱命题成立但**强命题不成立**，标题句要降级（§6.1）。
   风险不小：很可能 PE 用什么坐标的影响大于"是否与池化一致"。
   **提前想好降级后的写法，别临时硬撑**
2. **⚠️ novelty 表述被打** —— 见 1.1。三条硬事实必须自己先写进 related work，
   而不是等审稿人指出来
3. ~~stride 选错导致假阴性~~ —— 已定 stride=16（`docs/05` §8.7）
4. **深度回放发散**（§4.3 未验）—— 有回退方案，只是变贵
5. ~~多帧基线跑不动~~ —— 已排除，K=8 不压缩开检查点 **17.4 GB**（冻结视觉主干实测，`docs/05` §8.5）
6. **⚠️ 训练流程复现不到官方水平** —— 曾记为"已排除"，**满量评测推翻了它**：
   自建 step20000 = **74.0%**，官方 = **84.2%**，−10.2 点，**p=7.3e-05**
   （`docs/05` §8.2）。n=50 时的 72.0% vs 76.0%、p=0.65 是子集难度偏差造成的假象。
   **内部有效性不受影响**（五组共用同一配方，偏移抵消），
   但绝对数字不能与文献并列，且头部空间变大会让"加东西就涨"更容易发生
   —— **若 G2 vs G0 出现大幅提升，被砍掉的 G5 假历史对照必须捡回来**（§6）

**下一个决策点**：**G2 vs G0**（同预算下历史帧有没有用）。前置三件做完即可开跑。
⚠️ 不是 G1 vs G0——那一对差了 8 倍 token，赢了也说明不了问题（3.0.1）。
