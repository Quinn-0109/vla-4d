# 实验产物
# 轨迹 JSON 与 MP4 体积巨大（单个 suite 500 局），不入库；
# 但结果表和图表是结论本身，体积小，应当随代码一起版本化 —— 故下面放行。
results/*
!results/tables/
results/tables/*
!results/tables/*.csv
!results/tables/*.json
!results/figures/
results/figures/*
!results/figures/*.png
# 评测日志：每个几十 KB 的纯文本，却是阶段 0/1 全部结论的原始出处。
# 轨迹 JSON 与 MP4 太大不入库，日志属于"结论本身"那一类，应当版本化。
# ⚠️ 当初漏了这条 —— 30 个日志在训练机上只有一份拷贝，而那台机器的
#    results/ 已经丢过一次东西（深度缓存）。
!results/logs/
results/logs/*
!results/logs/*.txt

rollouts/
runs/                 # 微调输出: adapter + 优化器状态 + metrics.jsonl
experiments/logs/
wandb/

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# 模型权重与数据
*.pt
*.bin
*.safetensors
checkpoints/
# ⚠️ 必须带前导斜杠：不带的话 `data/` 会匹配**任意层级**的 data 目录，
# 把 `src/data/`（K 帧数据模块）一起吞掉 —— 而 `git add -A` 不会有任何提示，
# 提交看起来成功，文件却从没进过仓库。本项目栽过一次：kframe.py 写完、
# 自检跑过、docs 里引用了它，但机器上 git pull 下来根本没有这个文件。
/data/

# 训练机上会冒出来的杂物
MUJOCO_LOG.TXT
*.whl
.ipynb_checkpoints/
# ⚠️ 上面这条附个原因：用 Jupyter 打开仓库里的脚本，它会存副本、也可能改写原文件。
#    本项目已经因此丢过一次内容 —— finetune_kframe.py 被截断成 0 字节，
#    `git diff` 显示 259 行全删。**别用 Jupyter 编辑 scripts/ 下的文件。**
